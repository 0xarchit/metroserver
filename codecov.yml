coverage:
  precision: 1
  round: down
  range: "80...100"
  status:
    project:
      default:
        target: 80%
        threshold: 0%
        if_not_found: failure
    patch:
      default:
        target: 80%
        threshold: 0%
        if_not_found: failure

ignore:
  - "proto"
